/** Loader options interface to customize spinner behavior  */
export interface LoaderOptions {
  /**
   * If true, the loader will be displayed in fullscreen mode
   * TODO: Implements this feature
   */
  fullscreen?: boolean;
}
