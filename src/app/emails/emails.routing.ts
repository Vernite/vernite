import { Routes, RouterModule } from '@angular/router';

/**
 * Emails routes list
 */
const routes: Routes = [{}];

/**
 * Emails routes module
 */
export const EmailsRoutes = RouterModule.forChild(routes);
