import { Routes, RouterModule } from '@angular/router';

/**
 * Auth routes list
 */
const routes: Routes = [{}];

/**
 * Auth routes module
 */
export const AuthRoutes = RouterModule.forChild(routes);
