import { Routes, RouterModule } from '@angular/router';

/**
 * Tasks routes list
 */
const routes: Routes = [{}];

/**
 * Tasks routes module
 */
export const TasksRoutes = RouterModule.forChild(routes);
