/* tslint:disable:no-unused-variable */

import { TaskTypeIconPipe } from './task-type-icon.pipe';

describe('Pipe: TaskTypeIcone', () => {
  it('create an instance', () => {
    let pipe = new TaskTypeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
