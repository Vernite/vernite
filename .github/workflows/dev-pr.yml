name: Development PR checks

on:
  workflow_dispatch:
  pull_request:
    branches:
      - dev

jobs:
  build:
    uses: ./.github/workflows/_build.yml
    with:
      cache: ${{ github.run_id }}
    secrets: inherit

  test:
    uses: ./.github/workflows/_test.yml
    secrets: inherit
