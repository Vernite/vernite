name: Unit Tests

on:
  push:
    branches: [master, dev]
  workflow_dispatch:
  workflow_call:

jobs:
  run-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - uses: ./.github/workflows/prepare-environment.yml

      # Run unit tests
      - name: Unit tests
        run: yarn unit:build

      # Upload artifacts
      - name: Upload artifacts
        uses: actions/upload-artifact@v2
        with:
          name: unit-tests
          path: coverage/vernite

